# Regression log

Every bug that has regressed silently — fixed and reappeared, or was
silently introduced during a refactor — earns a row here with the
pinning test that now holds it down. A bug can be wrong in this repo at
most twice.

Columns:

- **Bug**: one-line description.
- **First seen**: when the behavior broke the first time (commit SHA
  or date).
- **Re-seen**: when it broke again, if applicable.
- **Root cause category**: the anti-pattern, so similar bugs can be
  recognized.
- **Pinning test**: the test file and (where useful) test name that
  will turn red if the bug reappears.
- **Rule**: the one-sentence do-not-do rule that also appears in
  `AGENTS.md`.

| Bug | First seen | Re-seen | Root cause category | Pinning test | Rule |
| --- | ---------- | ------- | ------------------- | ------------ | ---- |
| Divine-name substitution silently stopped applying on insert for upgrading users. | rewrite branch introduction of `apply_sheimot_on_insertion` (~2026-02) | — | **new opt-in gate without migration**: a new preference key was added defaulting to `false`; existing users who had substitutions on before never got them turned back on. | `test/tests/hebrew-preferences.test.js` — "applies Hebrew divine-name replacements after display normalization" pins the gate-on path; `test/tests/migrations.test.js` — "upgrading user without apply_sheimot_on_insertion gets it set to 'true'" pins the upgrade path (Stage 2). | Never add a new preference key that gates existing behavior without a default of `true` *and* an `apply_sheimot_on_insertion`-style migration. |
| `extendedGemaraPreference` module-scope global set but only read on sidebar-open paths; Quick-Actions menu callers see stale `false`. | rewrite branch refactor of `openSharedSidebar_` | — | **dead-scoped global**: the setter is conditional, the reader runs unconditionally. | `test/tests/extended-gemara.test.js` — "reads `extended_gemara` at call time" and "the dead module-scope global is gone" (Stage 2). | Module-scope globals that hold preferences are a smell. Read preferences at call time, not at sidebar-open time. |
| `formatDataForPesukim` appends a newline even when `pesukim` is `false`, changing inserted-document spacing. | rewrite branch (`Code.gs:759`) | — | **output-shape change without a snapshot test**. | `test/tests/format-data-for-pesukim.test.js` — "pesukim=false: verses are joined as prose with a single space (no trailing newlines)" (Stage 2). | Insertion-path output has snapshot tests. Do not "simplify" formatting paths without first updating the snapshot. |
| "Refresh Sidebar" button in preferences calls `google.script.run.refreshSidebarAfterPreferences()` — server function did not exist, click silently no-opped. | unknown (the client call is at `apps-script/preferences/js.html:791`; the server binding was either renamed or never landed) | — | **server refactor renames/removes a function the client still calls**: the failure path is silent because no `withFailureHandler` is attached. | `test/ui/rpc-surface.test.js` — "every google.script.run.X call in client HTML resolves to a surface-listed function". The server binding was restored in Stage 2 (`refreshSidebarAfterPreferences` reopens the sidebar, giving a fresh session). | Every client `google.script.run.X` call must resolve to a server function listed in `docs/rpc-surface.json`. The contract test enforces it. |
| Session-modal positioning / help-card hover broke repeatedly (multiple commits in the branch history). | several rounds | several rounds | **CSS-token drift**: inline `<style>` values in entry templates duplicated tokens already defined in `apps-script/css/tokens.html`; copies drifted. | Stage 6 will consolidate inline styles into the token file. There is no test for CSS visuals; the guardrail is "do not duplicate values already in `css/tokens.html`" plus the Stage 6 consolidation. | Edit `apps-script/css/tokens.html` for spacing / color / sizing values. Do not add inline `<style>` overrides in entry templates. |
| Lexicon-search and voices-clipboard-icon re-breakage (multiple commits). | several rounds | several rounds | **client-side selector contracts not pinned**. The server RPC relies on DOM ids that the UI team would remove or rename during visual rework. | `test/ui/selector-contracts.test.js` + `test/ui/contracts/selector-contracts.json`. Stage 1.5 makes sure every id the server RPC relies on is listed. | Before deleting a DOM id or class, grep `selector-contracts.json` for it. If it's listed, the rename must be bidirectional. |
| `insertReference` accepted nine positional boolean/string parameters; client-side call sites passed them in fixed order and an argument flip silently changed insertion behavior (e.g. passing the title as the language, or the transliteration flag as `insertCitationOnly`). | ongoing pressure across the rewrite | — | **too many positional parameters**: with 9+ params the compiler can't help you when two are reordered; "silent wrong insertion" is the failure mode. | `docs/rpc-surface.json` now pins `insertReference` at arity 2 (`data, opts`). The contract test catches accidental regressions to positional form. | Any server function with more than three parameters takes an options bag. `data` + `opts` is the convention; arity ≤ 2 is enforced by the RPC-surface contract. |
| Multi-version insertion (`insertReferenceVersions`) added to RPC surface; client calls it only in `he-top` and `en` output modes. Removing or renaming it would silently no-op multi-version inserts. | 2026-05-02 | — | **new RPC surface entry**: added `insertReferenceVersions` (arity 2) to `docs/rpc-surface.json` in the same commit as the server function. | `test/ui/rpc-surface.test.js` — pins arity and presence; client `google.script.run.insertReferenceVersions` call will fail the contract test if the server function is removed. | Any new `google.script.run.X` call in client HTML must have a matching entry in `docs/rpc-surface.json`. |
| `onInstall` and `onOpen` simple triggers placed inside `apps-script/server/menu.gs`; clasp pushed them with a slash-prefixed filename (`server/menu`), and the editor add-on (`appsscript.json` `addOns.docs`) failed to invoke them — the menu never built and the add-on appeared dead. | Stage 3 cutover (commit `30c1b36`, pushed via clasp) | — | **simple triggers in a subdirectory file**: clasp preserves directory structure as a slash-named filename in Apps Script; trigger discovery for editor add-ons is unreliable from those files. | `apps-script/triggers.gs` is the only file that defines `onInstall` and `onOpen`; `pre_clasp_qc.sh` check 1 (no duplicate basenames) plus a manual `grep -rn '^function on(Install\|Open)' apps-script/` confirms they are top-level. There is no automated test that proves trigger discovery — the guardrail is "simple triggers stay at the apps-script/ root". | Simple-trigger functions (`onInstall`, `onOpen`, `onEdit`, `onSelectionChange`, etc.) MUST live in a `.gs` file at the root of `apps-script/`. Domain logic stays in `server/`; entry points do not. |
| `onInstall` seeded preferences from a second hand-maintained literal that had drifted from `getDefaultPreferences()`: it omitted ~14 keys and disagreed on `translation_font_size` / `transliteration_font_size` (11 vs 12). A fresh install and a reset-to-defaults produced differently formatted documents. | rewrite branch (`apps-script/triggers.gs` `onInstall`) | — | **two sources of truth for one contract**: the defaults list was duplicated instead of referenced, so every new preference had to be added in two places and silently diverged when it wasn't. | No automated test yet. `onInstall` now builds its payload from `getDefaultPreferences()` plus an explicit, commented `getFreshInstallPreferenceOverrides_()`; a future test should assert `Object.keys(onInstall payload) ⊇ SETTINGS`. | Never write a second literal list of preference defaults. `getDefaultPreferences()` is the source of truth; deliberate fresh-install differences go in `getFreshInstallPreferenceOverrides_()` with a comment saying why. |
| `test/ui/snapshots/preferences.html.snap` was hand-edited to add the Quick Access section instead of being regenerated, so it silently diverged from the template (Hebrew combining-mark order and a `&ldquo;` entity) and `npm test` was left red on the branch. | 2026-08-26 (`4a58f3f`, following `456d378`) | — | **snapshot updated by hand**: the whole point of a byte-for-byte snapshot is that a human never types it; hand-editing reintroduces exactly the drift it exists to catch. | `test/ui/template-snapshots.test.js` already pins this — it was red and had to be made green rather than the snapshot being trusted. | Never hand-edit a file under `test/ui/snapshots/`. Regenerate with `UPDATE_UI_SNAPSHOTS=1 npm test` and read the resulting diff. |
| `insert_from_selection_at_top` added a pinned "Insert Source from Selection" menu item without removing the unconditional one, so enabling the preference showed the item twice. | `cbc5b97` (2026-08) | — | **new preference adds a branch but does not make the old branch exclusive**: the "on" path was added, the "off" path was never made mutually exclusive. | No automated test (menu construction needs the `DocumentApp` UI service). `buildAndInstallMenu` now guards the second `addItem` with `if (!insertAtTop)`. | When a preference relocates an existing UI element, the original site must become conditional in the same commit. Adding the new site alone duplicates it. |
| `expandShevaApostrophe` expanded EVERY consonant-apostrophe pair, so English possessives were mangled before any lookup ran: "A Woman's Commentary" became "A Womanes Commentary", "Jacob's Ladder" became "Jacobes Ladder", "don't" became "donet". Searching for a valid Sefaria title returned zero results. | introduction of the sheva-apostrophe helper in `search-utils.html` | — | **over-broad normalization on a shared input path**: `inputExpanded` feeds the /api/name lookup, the direct-ref resolve AND the content search, so one bad rewrite silently zeroed all three at once. The failure mode is "no results", which reads as "Sefaria doesn't have it" rather than as a bug. | `test/tests/search-input-normalization.test.js` — pins both directions: transliterated sheva still expands (P'sukei → Pesukei), possessives and contractions do not. | A normalization rule applied to raw user input must be anchored to the shape it actually targets. A transliterated sheva is word-initial after 1-2 consonants; nothing else may match. |
| `applyTypographyToParagraph` called `setBold(0, len - 1, false)` across the whole paragraph immediately after `insertRichTextFromHTML` had set per-run emphasis from Sefaria's markup, flattening it. The Steinsaltz Talmud lost the bolding that separates the Talmud's own words from Steinsaltz's interpolated explanation. | rewrite branch | — | **a later blanket write undoes an earlier per-run write**: both functions were individually correct; the bug lived only in their ordering, which no single function's tests could see. | No unit test (needs `DocumentApp`). `applyTypographyToParagraph` now takes an explicit `preserveSourceEmphasis` opt-in, captures emphasis runs via `getTextAttributeIndices()` before applying the baseline style, and re-asserts them after. `test/tests/migrations.test.js` pins the v6 migration that turns the preference on for upgraders. | Any function that writes an attribute across a whole range must state what it is allowed to overwrite. If source markup set it, the blanket write is a bug. |
| The migrations test hardcoded the current schema version as `'5'` in five separate assertions, so bumping the version broke five unrelated tests and trained the reader to edit the test to match the code. | 2026-08 | — | **a constant duplicated between code and test**: the test stopped describing intent and started restating an implementation detail. | `test/tests/migrations.test.js` now parses `PREFS_SCHEMA_CURRENT_` out of the source. Version-specific tests still pin their own `from` version explicitly, which is the part that carries meaning. | A test may hardcode the version it is migrating FROM. It must never hardcode the version the code currently targets. |
| Zero-result searches offered no suggestions for any query that was not the START of a catalogue title. "A Woman's Commentary" produced an empty panel even though "The Torah: A Women's Commentary" exists. | original fuzzy suggester in `search-utils.html` | — | **a performance prefilter silently became a correctness rule**: the same-first-character gate exists to keep Levenshtein off thousands of titles, but nothing recorded that it also decided which queries could ever get a suggestion. | `test/tests/search-input-normalization.test.js` — pins the fragment case, the suffix-carrying case, and (equally important) that unrelated prose still yields nothing. | A cheap prefilter in front of an expensive match must be justified as a *correctness* rule, not just a speed one. If it can exclude a right answer, it needs a second pass behind it. |
| `applyTypographyToParagraph` was called from 27 sites with four positional arguments, so adding colour meant either a fifth positional or touching every site. | accumulated | — | **positional parameters on a widely-called helper**: the same anti-pattern the `insertReference` row records, at a different scale. | No test. All call sites now go through `applyRoleTypography_(paragraph, typography, role, overrides)`; the positional form survives only as the primitive it wraps. | A helper called from more than a handful of sites takes a role name plus an options bag, not a growing argument list. |
| Traditional citation abbreviations resolved to nothing: "Hil. Shabbat 1:1" is how Rambam is cited, but Sefaria indexes the section as "Mishneh Torah, Hilchot Shabbat". The failure looked like a missing text rather than an unrecognized abbreviation. | pre-existing | — | **the reference resolver only understood the catalogue's own spelling**: every fallback in `resolveReferenceWithFallbacks` was a punctuation/whitespace variant of what the user typed, so no amount of retrying could bridge a naming-convention difference. | `test/tests/citation-abbreviations.test.js` — pins the expansions AND the invariant that no candidate ever drops a word the reader typed. | A reference expansion may only ADD words. Dropping one can silently retarget the query at a different work that really exists ("Hil. Avodah Zarah" → the Talmud tractate), which is worse than returning nothing. |
| "Link Texts with Sefaria" reported only what it linked. Ambiguous citations were linked to `refs[0]` — an arbitrary pick from an unranked list, presented as a result — and unresolvable ones disappeared entirely, so "Linked 4 references" was the whole report whether 4 or 40 citations were found. | pre-existing (published add-on) | — | **a summary that counts only successes**: the code had three outcomes (linked / ambiguous / unresolvable) and a message shaped for one, so two of them were unobservable from the UI and from the logs. | `test/tests/linker-classify.test.js` — pins each outcome separately, including that an already-linked range is skipped WITHOUT inflating the failure count, and that ambiguous candidates keep Sefaria's own order rather than being re-ranked. | When an operation has more than one non-success outcome, the summary reports each of them. A count of successes alone cannot be distinguished from silent failure by the person reading it. |

## Process

- Add a row in the same PR that fixes the regression and ships the
  pinning test. The regression log row is part of the fix.
- If an existing row's "Re-seen" column fills up, treat that as a
  signal the pinning test is insufficient or the rule is not yet
  mechanically enforced. Either strengthen the test or promote the
  rule into `pre_clasp_qc.sh`.
- When a test gets stronger and a rule becomes mechanically
  enforceable, move the row into `docs/resolved-regressions.md` with
  a pointer to the enforcing check. (This file does not exist yet;
  create it the first time you need it.)
